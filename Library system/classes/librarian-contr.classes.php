<?php

include "dbh.classes.php";

// the librarian has no properties so only a librarian controller is needed
class LibrarianContr extends DatabaseConnection {
    function check_fines($member_id) {
    }
}
